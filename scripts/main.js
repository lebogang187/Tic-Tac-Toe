const Gameboard = () => {
  let gameBoard = ["", "", "", "", "", "", "", "", ""];
};

const createPlayer = (name, mark) => {
  return { name, mark };
};

const Game = () => {
  let players = [];
  let currentPlayer;
};
